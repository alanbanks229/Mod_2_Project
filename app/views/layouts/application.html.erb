<!DOCTYPE html>
<html>
  <head>
    <title>Mod2Project</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Dancing+Script&display=swap" rel="stylesheet">
  </head>

  <body>
  <div class="navigation">
        <div class="ui animated fade button biggerButton" tabindex="0">
        <div class="visible content logo">Askit</div>
          <div class="hidden content">
            <span><%= link_to "About", about_path %>
          </div>
        </div>
    <div class="ui animated fade button biggerButton home" tabindex="0">
        <div class="visible content">Users</div>
          <div class="hidden content">
            <span><%= link_to "Users Index", users_path%>
          </div>
        </div>

    <div class="ui animated fade button biggerButton home" tabindex="0">
        <div class="visible content">Categories</div>
          <div class="hidden content">
            <span><%= link_to "Your Page", categories_path %>
          </div>
        </div>

    <% if session[:user_id] %>
      <div class="ui animated fade button biggerButton home" tabindex="0">
        <div class="visible content">Profile</div>
          <div class="hidden content">
            <span><%= link_to "Your Page", user_path(current_user) %>
          </div>
        </div>
      <div class="ui animated fade button biggerButton home" tabindex="0">
        <div class="visible content">Logout :)</div>
          <div class="hidden content">
            <span><%= link_to "Logout", "/logout" %>
          </div>
        </div>
      </div> <!--Navigation class ends here -->
      <h4>Logged in as: <%= current_user.username %></h4>
        <% if current_user.admin == true %>
          <h5 class="red"><b>ADMIN</b></h5>
        <% end %>
    <br>
    <% else %>
      <div class="ui animated fade button biggerButton home" tabindex="0">
        <div class="visible content"><i class="user plus icon"></i></div>
          <div class="hidden content">
            <span><%= link_to "Create Account", new_user_path %>
          </div>
        </div>
      <div class="ui animated fade button biggerButton home" tabindex="0">
        <div class="visible content">Login</div>
          <div class="hidden content">
            <span><%= link_to "Sign In", login_path %>
          </div>
        </div>
      </div> <!-- Navigation class ends here for else statement -->
      <h4>You are currently not logged in.
        <br><%= link_to "Create an account", new_user_path %> or <%= link_to "Login", login_path %>
        <br>To get the most out of this app.
      </h4>
      <% end %>   
  </div>





    <div class="header wrap_it_up">
    <div class="top-bar">
    
    </div>
    <br>
    </div>
    <%= yield %>
  </body>
</html>