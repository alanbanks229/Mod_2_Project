<h1>Create a New Category</h1>
<%= form_for @category do |c| %>
	<%= c.label :name %>
	<%= c.text_field :name %> 
	<br>
	<br>
	<%= c.label :description %>
	<%= c.text_area :description%>
	<br>
	<br>
	<%= c.submit %>
	<% end %>

